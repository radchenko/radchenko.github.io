{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "chrome",
      "request": "launch",
      "name": "Launch Website",
      "preLaunchTask": "Serve Website",
      "url": "http://localhost:8080",
      "webRoot": "${workspaceFolder}"
    }
  ]
}